{
  "name": "straystation-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:css": "tailwindcss -i ./input.css -o ./assets/tailwind.css --watch",
    "build:css": "tailwindcss -i ./input.css -o ./assets/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.18"
  }
}
